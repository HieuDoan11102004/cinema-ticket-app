# Non-sensitive configuration
# Sensitive values (passwords, secrets) go in .env

database:
  user: user
  db: cinema
  host: localhost
  port: 5432

jwt:
  algorithm: HS256
  JWT_ACCESS_TOKEN_EXPIRE: 30m
  JWT_REFRESH_TOKEN_EXPIRE: 168h
